From 27da8464535b6215652464241a4c0baa90115f70 Mon Sep 17 00:00:00 2001
From: Tim Kordas <tim.kordas@joyent.com>
Date: Wed, 11 Oct 2017 15:20:04 -0700
Subject: [PATCH] joyent/node-cueball#132 Expose counters and simple metrics
 from CueBallConnectionPool

---
 lib/pool.js | 12 ++++++++++++
 1 file changed, 12 insertions(+)

diff --git a/lib/pool.js b/lib/pool.js
index 84cab63..9466a16 100644
--- a/lib/pool.js
+++ b/lib/pool.js
@@ -729,6 +729,18 @@ CueBallConnectionPool.prototype.printConnections = function () {
 	console.log('dead:', self.p_dead);
 };
 
+CueBallConnectionPool.prototype.getStats = function () {
+	var stats = {};
+
+	stats['counters'] = self.p_counters;
+	stats['totalConnections'] = Object.keys(self.p_connections).length;
+	stats['idleConnections'] = self.p_idleq.length;
+	stats['pendingConnections'] = self.p_initq.length;
+	stats['waiterCount'] = self.p_waiters.length;
+
+	return (stats);
+};
+
 CueBallConnectionPool.prototype.claim = function (options, cb) {
 	var self = this;
 	var done = false;
-- 
2.21.0

