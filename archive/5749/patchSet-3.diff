commit 94bc071e8912071714d052fbfa15afaeeca83da7
Author: YanChii <janci@binaryparadise.com>
Date:   2019-04-06T16:15:50+00:00 (6 months ago)
    
    fix qemu detecting a 64-bit host (related to live migration)

diff --git a/qemu-kvm-x86.c b/qemu-kvm-x86.c
index cb3dad0..4491ed5 100644
--- a/qemu-kvm-x86.c
+++ b/qemu-kvm-x86.c
@@ -508,7 +508,16 @@ int kvm_arch_qemu_create_context(void)
     struct utsname utsname;
 
     uname(&utsname);
-    lm_capable_kernel = strcmp(utsname.machine, "x86_64") == 0;
+    if(strcmp(utsname.sysname, "SunOS") == 0) {
+        // uname in illumos doesn't provide info about 32/64-bit version of booted kernel
+#if defined(_LP64) || defined (__amd64)
+        lm_capable_kernel = 1;
+#else
+        lm_capable_kernel = 0;
+#endif
+    }
+    else
+        lm_capable_kernel = strcmp(utsname.machine, "x86_64") == 0;
 
     if (kvm_shadow_memory) {
         kvm_set_shadow_pages(kvm_context, kvm_shadow_memory);
