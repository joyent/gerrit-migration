From 0087047f1fcd30640f487ca3c4c848a304021736 Mon Sep 17 00:00:00 2001
From: Brian Bennett <brian.bennett@joyent.com>
Date: Tue, 27 Aug 2019 18:08:53 -0700
Subject: [PATCH] joyent/manta-thoth#177 Installer should be more clear about
 which $SDC_ACCOUNT and $SDC_KEY_ID to use

---
 bin/sdc-thoth-install | 4 ++--
 1 file changed, 2 insertions(+), 2 deletions(-)

diff --git a/bin/sdc-thoth-install b/bin/sdc-thoth-install
index 6d5e7de..489fecf 100755
--- a/bin/sdc-thoth-install
+++ b/bin/sdc-thoth-install
@@ -92,8 +92,8 @@ if [[ -z $DOWNLOAD_MANTA ]]; then
 
 fi
 
-[[ -n $SDC_ACCOUNT ]] || fatal "must set \$SDC_ACCOUNT"
-[[ -n $SDC_KEY_ID ]] || fatal "must set \$SDC_KEY_ID"
+[[ -n $SDC_ACCOUNT ]] || fatal "must set \$SDC_ACCOUNT. This is the account in manta that stores your thoth data."
+[[ -n $SDC_KEY_ID ]] || fatal "must set \$SDC_KEY_ID. This must already exist in the $SDC_ACCOUNT account, and needs to be available on this node (ssh-agent is recomended)."
 
 if [[ -z $SDC_URL ]]; then
 	server=us-west-1.api.joyentcloud.com
-- 
2.21.0

